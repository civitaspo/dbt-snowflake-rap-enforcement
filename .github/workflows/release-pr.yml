name: Release PR

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions: {}

concurrency:
  group: release-pr
  cancel-in-progress: false

jobs:
  prepare:
    name: prepare release pull request
    if: |
      github.event_name == 'workflow_dispatch' ||
      !startsWith(github.event.head_commit.message, 'chore(release):')
    runs-on: ubuntu-latest
    permissions:
      contents: read
    timeout-minutes: 10
    env:
      SECUREFIX_CLIENT_APP_ID: ${{ vars.SECUREFIX_CLIENT_APP_ID }}
      SECUREFIX_SERVER_REPOSITORY: ${{ vars.SECUREFIX_SERVER_REPOSITORY }}

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Set up mise
        uses: jdx/mise-action@7e36c90d9ab29c415a2384db3006f3ec8a8cc654 # v4.2.4
        with:
          version: "2026.8.3"
          install_args: "--locked"
          cache: false

      - name: Compute release
        id: release
        run: |
          set -euo pipefail
          version="$(scripts/release/prepare.sh | sed -n 's/^VERSION=//p' | tail -n1)"
          if [ -z "$version" ]; then
            echo "No releasable commits found."
            echo "releasable=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "releasable=true" >> "$GITHUB_OUTPUT"

      - name: Require Securefix configuration
        if: |
          steps.release.outputs.releasable == 'true' &&
          (env.SECUREFIX_CLIENT_APP_ID == '' || env.SECUREFIX_SERVER_REPOSITORY == '')
        run: |
          echo "Securefix client variables are not configured." >&2
          exit 1

      - name: Request release pull request
        if: |
          steps.release.outputs.releasable == 'true' &&
          env.SECUREFIX_CLIENT_APP_ID != '' &&
          env.SECUREFIX_SERVER_REPOSITORY != ''
        uses: csm-actions/securefix-action@4d885e1bcb71f4f110215c833002ce9fa0ee0fa6 # v0.6.0
        with:
          action: client
          app_id: ${{ env.SECUREFIX_CLIENT_APP_ID }}
          app_private_key: ${{ secrets.SECUREFIX_CLIENT_PRIVATE_KEY }}
          server_repository: ${{ env.SECUREFIX_SERVER_REPOSITORY }}
          files: |
            .release-version
            CHANGELOG.md
          use_git_ls_files: "false"
          branch: release/next
          commit_message: "chore(release): prepare v${{ steps.release.outputs.version }}"
          pull_request_title: "chore(release): v${{ steps.release.outputs.version }}"
          pull_request_base_branch: main
          pull_request_body: |
            ## Summary
            - Prepare release v${{ steps.release.outputs.version }}
            - Update the changelog and release metadata

            This pull request is managed by the Release PR workflow.
