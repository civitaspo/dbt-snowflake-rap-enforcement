[project]
name = "dbt-snowflake-rap-enforcement-tests"
version = "0.3.1"
description = "Test tooling for dbt-snowflake-rap-enforcement"
requires-python = ">=3.10"
dependencies = [
    "dbt-core>=1.12,<1.13",
    "dbt-duckdb>=1.11,<1.12",
]

[project.optional-dependencies]
snowflake-e2e = [
    "dbt-snowflake>=1.12,<1.13",
]

[tool.uv]
package = false
