[tools]
"aqua:astral-sh/uv" = "0.11.16"
"aqua:koalaman/shellcheck" = "0.11.0"
"aqua:suzuki-shunsuke/disable-checkout-persist-credentials" = "1.0.0"
"aqua:suzuki-shunsuke/ghalint" = "1.5.6"
"aqua:suzuki-shunsuke/pinact" = "4.1.1"

[tasks.lint]
description = "Run local lint checks"
run = """
#!/usr/bin/env bash
set -euo pipefail
if [ -d scripts ]; then
  find scripts -type f -name '*.sh' -print0 | xargs -0 -r shellcheck
fi
if [ -d .github/workflows ]; then
  ghalint run
  pinact run --check
fi
"""

[tasks.test]
description = "Run package tests"
run = """
#!/usr/bin/env bash
set -euo pipefail
if [ ! -f dbt_project.yml ]; then
  echo "dbt_project.yml not present yet; skipping tests"
  exit 0
fi
if [ ! -d unit_tests ] && [ ! -d integration_tests ]; then
  echo "No unit_tests/ or integration_tests/ yet; skipping tests"
  exit 0
fi
echo "Test harness not implemented yet" >&2
exit 1
"""
