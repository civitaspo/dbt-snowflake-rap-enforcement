# dbt-snowflake-rap-enforcement

A dbt package that applies Snowflake row access policies (RAP) to models—including existing tables—and keeps RAP-side policy for downstream references.

This repository is under initial OSS bootstrap.
